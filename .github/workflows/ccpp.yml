name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: install
      run: apt install -y extra-cmake-modules libwayland-dev wayland-protocols
    - name: configure
      run: mkdir build && cd build && cmake ..
    - name: make
      run: cmake --build build
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
